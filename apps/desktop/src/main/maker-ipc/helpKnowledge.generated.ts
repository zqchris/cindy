// AUTO-GENERATED by scripts/gen-help-kb.mjs — do not edit by hand.
// Source: apps/desktop/help-knowledge/*.md  ·  Regenerate: pnpm gen:help-kb
import type { HelpTabId } from '../../shared/helpTypes.js';

export interface HelpKnowledgeDoc {
  id: string;
  title: string;
  summary: string;
  tab?: HelpTabId;
  content: string;
}

export const HELP_KNOWLEDGE: HelpKnowledgeDoc[] = [
  {
    id: "appearance",
    title: "Appearance, theme, language and notifications",
    summary: "Theme, theme family, fonts, display language, and desktop / FeiShu notifications — all in Settings > General.",
    tab: "general",
    content: "Settings > General controls how the app looks and how it notifies you.\n\n**Appearance:**\n\n- **Theme**: light, dark, or system (follows your OS).\n- **Theme family**: pick from the registered theme families; each family supplies its own colors for light and dark. The app uses a VSCode-style token system (see docs/design-rules/cindy-design-system.md) so themes only override what they need.\n- **Fonts and sidebar density**: the same section also has UI / code font-family pickers with font-size sliders, and a selector for the sidebar's session card mode.\n- **Local theme brand identity**: this is an optional power-user feature configured in the local theme JSON. The settings page intentionally keeps only create-copy, open-folder, and refresh actions. New copies include self-explanatory example paths directly in the JSON.\n- **Export / open local theme files**: export the current theme's tokens to a file or open a local theme JSON for inspection / sharing. To replace the icon and logo used on the new-chat page:\n\n  ```json\n  {\n    \"brand\": {\n      \"icon\": \"/absolute/path/to/your-image-folder/icon-square-50x50px.png\",\n      \"logo\": \"/absolute/path/to/your-image-folder/logo-horizontal-110x37.5px.png\"\n    }\n  }\n  ```\n\n  The example filenames describe the final display areas; source images may be exported at 2x/3x while keeping the same aspect ratios. Paths must be absolute JSON strings; spaces do not need shell quotes. PNG, JPEG, and WebP are supported. Transparent padding is trimmed at render time without changing the source file. Only `brand.icon` and `brand.logo` are recognized; the previous single-image fields are not compatible with the redesigned two-asset layout.\n\n**Language:**\n\n- Display language: System / 中文 / English / 日本語 / 한국어. Affects UI text only; agent replies follow your prompt and personalization, not this setting.\n\n**Notifications:**\n\n- **Desktop notification on session finished** — OS-level ping when an agent completes its reply.\n- **FeiShu DM on session finished** — requires the FeiShu bot to be configured (see the FeiShu bot topic).\n\n**Notes:**\n\n- Theme changes apply immediately; no restart needed.\n- Language changes apply immediately, but a few static strings may need a window reload to update.",
  },
  {
    id: "attachments",
    title: "Attaching files and images, and referencing code",
    summary: "Paste / drag images, @-mention files, and add extra reference directories from the composer.",
    content: "You can give the agent more than plain text.\n\n**Images:**\n\n- **Paste** an image from the clipboard, or **drag** image files onto the composer.\n- Accepted formats: **PNG, JPEG, WEBP, and GIF**.\n- Thumbnails appear above the input; click one to open a full-screen lightbox preview (remove from there, or ESC to close).\n\n**`@`-mention to reference files / directories / agents:**\n\n- Type `@` to open the picker. It shows files, directories, and registered agents in one flat list.\n- Scope: the picker scans your session's **working directory** (extra reference directories are given to the agent as context but aren't part of the `@` picker's file scan).\n- Matching is fuzzy, with a boost for prefix matches on the filename.\n\n**Extra reference directories (read-only context for the agent):**\n\n- Open the composer's **+** menu (to the left of the permission selector — it also hosts New Goal, Plan Mode, and Plugins) and add a directory from the OS folder dialog.\n- Per-session; up to **10** extra directories. This section shows for **Claude Code** sessions (Codex ignores extra directories).\n- Directories that are subdirectories of the current working directory are silently skipped (already covered). Parent / ancestor directories are flagged with a warning.\n- The agent gets read-only context for these directories — it can grep / read them, but writes still go to the working directory.\n\n**Notes:**\n\n- Image attachments are sent with that user message only; they're not implicitly re-included on subsequent turns.\n- The `@`-mention just inserts a reference string into your prompt — it's the agent's job to actually open / read the referenced item.",
  },
  {
    id: "collaboration",
    title: "Collaboration mode (lead + workers)",
    summary: "Turn on Collab to bring one or more worker agents in beside your session and delegate tasks to them.",
    content: "Collaboration (\"协同\" / Collab) lets one session — the **lead** — bring in one or more **worker** agents to run alongside it (a \"team\"). The lead delegates tasks to workers through a built-in MCP, and you can watch and steer each worker.\n\n**Turning it on:**\n\n- In a project session — local or SSH remote — click the **Collab** pill in the composer toolbar. Both Claude Code and Codex sessions can be the lead, on either side.\n- Pick the first worker's agent (Claude Code or Codex), and optionally its role / model / effort / an initial task.\n- Start. This creates the team plus that first worker.\n\n**Adding more workers:**\n\n- From the collaboration tab's worker list, use **Create new worker** (+) to add more.\n- Default limits are a **soft cap of 5** and a **hard cap of 8** workers per lead — you'll see a warning past the soft cap, and creation is blocked at the hard cap. Both are adjustable (1–20) in the Collaboration section under **Settings > General**.\n\n**While collaborating:**\n\n- Each worker is a full session with its own history, tools, working directory, and model, and runs with permissions bypassed so it doesn't stop to ask. It inherits the lead's working directory by default (or shares the worktree when the lead uses one).\n- A worker's model defaults to your last \"New Maker\" choice for that vendor, falling back to the lead's model.\n- Only one worker is **focused** (shown) at a time — switch between them with the worker tabs / dropdown. You can intervene in any worker directly, and archive workers individually.\n\n**Turning it off:**\n\n- Closing collab **confirms with you first** (in-progress work is lost), then ends the workflow and archives **all** the team's workers. The conversation history stays searchable.\n\n**Notes:**\n\n- The lead must be a Claude Code / Codex **project** session — local **or SSH remote**. On a remote lead, workers are spawned on the **same remote host**, and the collaboration MCP reaches the remote side over an SSH remote-forward to the local bridge (with a persistent bearer token). A worker can't start its own sub-collab (no nesting).\n- A lead can have only **one active collaboration workflow** at a time. If you have two clients open on the same account (e.g. dev + release) and start collab on the same session in both, the second fails with \"开启协同失败\" because a workflow is already active — close one client or end the existing collab.",
  },
  {
    id: "commands",
    title: "Slash commands",
    summary: "Type \"/\" in the composer to open a palette merging your skills, built-in app commands, and the agent's own commands.",
    content: "Type `/` at the start of the composer to open the command palette.\n\n**What's in the palette (priority order on collisions):**\n\n1. **Your installed skills** (highest priority) — anything in your global or project skills directory (see the Skills topic).\n2. **Built-in app commands** — `/help` (show available commands), `/clear` (clear the current session context — resets the conversation in place without creating a new session), `/cmd` (run a shell command in the working directory), `/issue` (file an issue), `/goal` (start a goal-driven run), `/learn`, `/workflows`, and `/jump-session`.\n3. **The agent's own commands** — e.g. `/compact`, `/agents`, `/memory` (the exact list depends on the agent; Claude Code and Codex each contribute their own).\n\n**Using the palette:**\n\n- Start typing to filter. Matching is **prefix-only** (case-insensitive `startsWith` on the command name) — not fuzzy.\n- Up to 25 matches shown at a time.\n- **↑ / ↓** move focus, **Enter** runs the focused command, **Esc** closes the palette.\n\n**Notes:**\n\n- If a name collides across sources (same command in a skill and in a built-in), the higher-priority source wins; the lower-priority one is silently skipped (logged as a warning).\n- Most built-in app commands run inside the desktop app (a few, like `/workflows` and `/jump-session`, are handled directly in the UI); agent commands are sent as prompt prefixes to the agent.",
  },
  {
    id: "connections",
    title: "Connecting external accounts",
    summary: "External accounts (Google, Jira / Confluence, GitHub, GitLab) are connected on their plugin detail pages under Plugins; Slack and Telegram are bound under Settings > IM bots; Codex sign-in lives in Settings > Model Providers.",
    content: "External account connections have moved out of the old \"Third-Party Platforms\" section — each integration now lives on its own plugin detail page under **Plugins**, and the plugin's tools use the account you connect there.\n\n**Where to connect what:**\n\n- **Slack** — bound with a single toggle under **Settings > IM bots** (the \"Cindy\" tab). One browser sign-in covers both directions: the Slack channel bot that receives tasks, and the agent Slack tools (search / read / post). If Slack tools don't work on an older binding, toggle it off and on once to re-authorize.\n- **Telegram** — under **Settings > IM bots** (the \"Cindy\" tab), turn on Telegram and choose **Link Telegram**. Open the one-time link, start the official Cindy bot, and complete confirmation if prompted. The same card lets you cancel, retry, open the bot, add it to a group, or unlink; no personal bot token is required.\n- **Google** (Gmail / Calendar / Drive / Sheets) — handled by the Filo Google plugin (Plugins > Filo Google).\n- **GitHub** — handled by the Cindy GitHub plugin (Plugins > Cindy GitHub; existing PAT connections are migrated automatically).\n- **GitLab** — handled by the Cindy GitLab plugin (Plugins > Cindy GitLab; existing PAT connections are migrated automatically).\n- **Codex** — sign in under Settings > Model Providers (separate from the Codex CLI binary the app ships with).\n- **FeiShu** — bot only (App ID / Secret under Settings > IM bots; see the FeiShu bot topic). FeiShu is no longer a sign-in method for Cindy itself.\n- **Cindy AI** — Settings > Model Providers. Web-search keys are configured on the matching plugin detail page (Cindy Web Search).\n\n**Managing connections:**\n\n- Each plugin detail page shows the connected accounts (with status) and lets you reconnect, disconnect, or switch the default account.\n- Disconnect removes the local token only; you may also want to revoke access on the provider's side.\n- A connection error usually means the token expired or was revoked on the provider's side; reconnect to refresh.",
  },
  {
    id: "feishu-bot",
    title: "Setting up the FeiShu (Lark) bot",
    summary: "Configure the FeiShu bot (App ID / App Secret) in Settings > IM bots to get DM notifications and bot interaction.",
    tab: "im-bot",
    content: "Cindy can send DMs and take commands through a FeiShu (Lark) bot. (This is unrelated to signing into Cindy — you sign in with your Cindy account; FeiShu is no longer a login method.)\n\n**Setting up the bot:**\n\n- Open Settings > IM bots, then the FeiShu Bot section.\n- Paste the bot's **App ID** and **App Secret**, then click **Bind**.\n- The status badge updates: **needs-config** → **connecting** → **connected** (or **conflict** / **error**).\n\n**Binding the bot to you:**\n\n- After the badge shows **connected**, the page instructs you to DM the bot a code (or click an action) to complete the binding. Until you bind, the bot won't know which FeiShu user to DM.\n\n**Notifications:**\n\n- Toggle **message notifications** on the same page to get bot DMs when sessions finish, or when the bot has updates for you.\n- The desktop-OS-level \"session finished\" notification is a separate toggle in **Settings > General > Notifications**.\n\n**Notes:**\n\n- The \"conflict\" badge state means another Cindy instance is already bound to the same bot for this user — typical when running dev + release simultaneously. Disconnect on one side to clear it.\n- Settings > IM bots also hosts the Slack and Telegram bindings (the \"Cindy\" tab) and a bring-your-own Discord bot, alongside this FeiShu bot.",
  },
  {
    id: "getting-started",
    title: "Getting started with Cindy",
    summary: "Create a session, choose an agent and working directory, send your first prompt, and iterate.",
    content: "Cindy is a desktop app that runs AI coding agents (Claude Code and Codex) for you.\n\n**Your first session:**\n\n- Click **+ New** in the top of the left sidebar.\n- Choose the **agent** (Claude Code or Codex).\n- For code work, choose a **working directory** — the agent runs its file ops there. (You can skip this for plain-Q&A sessions.)\n- Optionally pick a **model** in the composer toolbar.\n- Type your goal and send.\n\n**Iterating:**\n\n- A session is a persistent chat thread: ask, review the result, then ask the next thing.\n- While the agent is replying, the **Send** button becomes **Stop** — click it to interrupt. You can keep typing in the meantime; follow-ups queue up and send in order.\n- Type `/` in the composer to open the slash-command palette (see the Commands topic).\n\n**Organizing:**\n\n- All sessions are listed in the left sidebar (see the Sidebar topic). Switch between them by clicking; each session remembers its agent, model, and working directory.\n- Keep unrelated tasks in **separate** sessions — sharing one session for everything dilutes context.\n\n**Tone tip:**\n\n- Describe the goal and context first, then what result you want. That works better than a single short command. The agents respond well to \"here's what I'm trying to do, here are the constraints, here's what 'done' looks like\".",
  },
  {
    id: "import-sessions",
    title: "Importing Codex and Claude conversations",
    summary: "Scan and import existing Codex or Claude Code chat history from Settings > Session Import.",
    tab: "import",
    content: "If you already have Codex or Claude Code conversations on disk (from the CLIs), Import brings them into Cindy so they show up in the sidebar alongside your other sessions.\n\n**Importing:**\n\n- Open Settings > Session Import.\n- Click **Scan** to discover sessions in the standard Codex / Claude Code locations on your machine.\n- Filter the results:\n  - by **source** — Codex or Claude Code;\n  - by **placement** — by project (sessions inside a specific working directory) or by dialogue (free-form conversations not tied to a project).\n- Tick the sessions or projects you want, then click **Import**.\n- They appear in the left sidebar.\n\n**Notes:**\n\n- Importing is **one-way and non-destructive** — the originals on disk are left in place; nothing is synced back. Re-importing the same session is a no-op.\n- Imported threads are read-only history at first; you can continue them just like any other Cindy session.\n- This is for bringing in past conversations — it's unrelated to archiving / deleting sessions (see the Sidebar topic for those).",
  },
  {
    id: "integrations",
    title: "What Cindy integrates with",
    summary: "Supported external services — FeiShu, Slack, Telegram, Discord, Google, Jira / Confluence, GitHub, GitLab — and what's not supported.",
    content: "At a glance, Cindy integrates with:\n\n- **FeiShu (Lark)** — DM-style bot notifications and interaction (Settings > IM bots). See the FeiShu bot topic. (FeiShu is no longer a sign-in method for Cindy itself.)\n- **Slack** — one binding (Settings > IM bots, the \"Cindy\" tab) powers both directions: the Slack channel bot for receiving tasks, and agent Slack tools (search / read history / post as the bound user) available in every session.\n- **Telegram** — link your Telegram account to the official Cindy bot under Settings > IM bots (the \"Cindy\" tab). No bot token is required. Telegram tasks use the same local projects and Cindy sessions as Slack, while keeping channel-specific project and model preferences separate.\n- **Discord** — a Discord bot you bring yourself (your own Discord App + bot token) under Settings > IM bots.\n- **Google** — Gmail / Calendar / Drive / Sheets, via the Filo Google plugin (Plugins > Filo Google).\n- **GitHub / GitLab** — via the Cindy GitHub and Cindy GitLab plugins (Plugins).\n- **The coding agents** — Claude Code and Codex can reach pay-per-token models through **Cindy AI** in Settings > Model Providers.\n\n**Not supported:**\n\n- There is no built-in Microsoft Teams integration. For chat notifications, use the FeiShu, Slack, Telegram, or Discord bot.\n- There's no native Linear integration as a first-party feature — those would have to come via an MCP a user installs themselves.\n\n**Notes:**\n\n- Most of these connections are accessed by the agents through their tools (plugins / MCPs), not directly by the desktop app. Connecting on the plugin detail page is what gives its tools access to your account.",
  },
  {
    id: "memory",
    title: "Memory across conversations",
    summary: "The \"Cindy\" master switch turns on shared cross-agent memory (default on); when off, each agent uses its own native memory — all in Settings > Personalization > Memory.",
    tab: "personalization",
    content: "Memory lets the agents carry context across your conversations — facts about you, your projects, and how you like to work. There are two kinds, and one master switch chooses between them.\n\n**Where it lives:**\n\n- Settings > Personalization > Memory.\n\n**The \"Cindy\" master switch (default ON):**\n\n- Turning **Cindy** on enables a **shared, cross-agent memory** scoped to each working directory — Claude Code and Codex sessions in the same directory draw on the same memory.\n- While it's on, the per-agent native toggles below are **disabled / grayed out**, because the shared memory supersedes them.\n- It's **on by default** for new installs.\n\n**Per-agent native memory (used when the master switch is OFF):**\n\n- When Cindy is off, each agent falls back to **its own native memory** — so turning the master off does not mean \"no memory,\" it means Claude Code and Codex each remember things their own way.\n- Separate toggles let you enable / disable native memory for **Claude Code** and **Codex** independently (these are only interactive while the master switch is off).\n\n**Resetting:**\n\n- **Per-agent reset** clears a single agent's native memory.\n- **Reset all Cindy memory** clears the shared cross-agent memory for every working directory.\n- A section-level **reset to defaults** restores the switches themselves.\n- Resets ask for confirmation and aren't undoable.\n\n**What memory is, what it isn't:**\n\n- It's the agents' **long-term memory across sessions** — notes about you and your projects.\n- It's **not** the in-session chat history (that's always persisted to the session itself).\n- It's **not** your Personalization custom instructions (those are standing instructions you write; memory is what the agents record about you).\n\n**Notes:**\n\n- Changes take effect on the **next** chat / agent turn — toggling mid-conversation doesn't retroactively scrub current context.\n- Memory is stored locally and does not migrate if you change machines or wipe the app's user data.",
  },
  {
    id: "models-and-compat",
    title: "Choosing a model",
    summary: "Pick a session's model from the composer dropdown; non-Anthropic models work automatically (no compat toggle to flip).",
    tab: "providers",
    content: "You pick the model a session uses from the **model dropdown in the composer toolbar**. In the dropdown, models are organized under the source providers you've connected. The brand groups they belong to are **Anthropic**, **OpenAI**, **Budget GPT**, **xAI**, **Google**, and **Domestic** (Kimi / GLM / Qwen / DeepSeek), plus non-chat model kinds (image / audio / video / …).\n\nThe default depends on the agent: Claude Code defaults to **Opus 4.8**, Codex defaults to a **GPT** model.\n\n**Switching mid-session:**\n\n- Open the dropdown and pick a different model. It takes effect on the **next turn**.\n\n**Using non-Anthropic models:**\n\n- You don't need to flip anything. Claude Code's SDK emits Anthropic-specific request fields (`thinking`, `cache_control`, etc.) that non-Anthropic backends would reject, so the app **always** routes requests through a local proxy that scrubs those fields per-model before forwarding. Claude / Anthropic models pass through untouched.\n- (This used to be a manual \"Compat Mode\" toggle in Settings. That toggle has been retired — the handling is now automatic and always on, so there's nothing to configure.)\n\n**Notes:**\n\n- Which models you can pick depends on the providers connected in **Settings > Model Providers** (see the Providers topic).\n- \"Budget GPT\" is a lower-cost routing tier for GPT models; \"Domestic\" groups the China-hosted models.",
  },
  {
    id: "personalization",
    title: "Custom instructions (personalization)",
    summary: "Standing instructions the agents follow across every session — in Settings > Personalization.",
    tab: "personalization",
    content: "Personalization is where you give the agents standing instructions that apply to **every** session — tone, language, conventions, things you want the agent to always keep in mind.\n\n**Setting it up:**\n\n- Open Settings > Personalization.\n- Write your instructions in the text area, then save.\n- Useful examples: \"respond in Chinese\", \"default to TypeScript\", \"prefer pnpm over npm\", \"always check tests before reporting done\".\n\n**Other things on this page:**\n\n- **Memory** — long-term memory across sessions (see the Memory topic).\n- **Subagent models** — choose which models the agents' subagents use.\n- **Tips** — a few functional toggles (e.g. silent encrypted retry, chat embedding).\n\n**Notes:**\n\n- Changes apply to **new turns**; they don't rewrite previous messages in current sessions.\n- Per-session overrides (a stricter rule for one specific session) are best done in that session's first message, not here.\n- Don't put secrets here — instructions are sent with every prompt.",
  },
  {
    id: "plan-mode",
    title: "Reviewing and approving plans",
    summary: "When the agent proposes a plan, approve it, edit it, or send feedback before it proceeds.",
    content: "For larger tasks the agent may first propose a **plan**, shown as a card in the chat. You decide whether to approve, edit, or send feedback before any code runs.\n\n**Card controls:**\n\n- **Approve** — the agent receives the (possibly edited) plan and starts executing.\n- **Feedback** — opens a text row; what you write is sent back to the agent as a denial reason. The agent can then propose a revised plan or take a different path.\n- **Edit** — open the plan markdown in the card and edit it directly. Edits are auto-saved (debounced) to a plan file on disk; on Approve the edited version is what the agent uses.\n- **Cancel** (the toolbar **✕** or **Esc**) — dismiss the review and end the plan loop without approving or sending feedback.\n- **Expand / Half / Minimize** — change the card's display size to read or skim it.\n\n**How it appears:**\n\n- The agent emits a `plan_review` event with the plan markdown; the app renders the card in the chat thread and pauses the agent until you respond.\n\n**Notes:**\n\n- For Claude Code, this card is exactly how the SDK's `ExitPlanMode` tool surfaces: the app intercepts that tool call into a `plan_review` interaction and, on Approve, auto-exits plan mode. Cindy's renderer ↔ main `resolveInteraction` layer is what renders the card and returns your decision.\n- An approved plan card stays in your chat history with an \"approved\" state; you can scroll back to it.\n- If you close the window while a plan card is open, the agent is still paused waiting for your decision — reopen the session to resolve it.",
  },
  {
    id: "plugins",
    title: "Plugins (extending what the agents can do)",
    summary: "What plugins are, how to browse / install / enable / disable them, per-working-directory control, the permission model, and invoking one with a $command.",
    tab: "ghosts",
    content: "Plugins (internally \"意识\" / Ghost) extend what the agents can do — they add AI-callable tools, dockable UI panels, and \"ask Cindy to make it\" media generation. Each plugin is a self-contained `.cindy` capability pack that runs in its own isolated sandbox.\n\n**The Plugins page:**\n\n- Open it from the **Plugins** entry in the left sidebar.\n- Browse by origin: **all / built-in / team-shared / external**. The team-shared (enterprise) group only shows if your account belongs to an organization.\n- Each plugin has a detail page — that's where you enable it, connect an account / enter an API key, and read what it can do.\n\n**Built-in plugins that ship with the app:**\n\n- **Cindy Art** (`$cindy-art`) — generate and edit images and short videos.\n- **Cindy Web Search** (`$cindy-web-search`) — public web search (add your own Brave or Tavily key on its detail page).\n- **Cindy Mermaid** (`$cindy-mermaid`) — build / repair Mermaid diagrams.\n- **Cindy GitHub** (`$cindy-github`) / **Cindy GitLab** (`$cindy-gitlab`) — read / write issues, PRs / MRs, reviews, CI, releases.\n- **Filo Google** (`$filo-google`) — Gmail / Calendar / Drive / Sheets.\n\n**Enabling and disabling:**\n\n- **Enable / disable (globally)** from the plugin's detail page. Disabling puts its tools and panel to sleep but keeps the plugin installed — its layout slot is remembered and restored when you re-enable it.\n- **Disable for one working directory only** — a plugin can be turned off just for a specific project. In that project's sessions it behaves as if it isn't installed (its tools and `$command` are unavailable there); everywhere else it stays on.\n\n**Installing:**\n\n- Install from the marketplace, or add a `.cindy` file manually (drag it in / pick it from disk).\n- Every install or update shows a **permission confirmation dialog** that lists exactly what the plugin can access (network domains, credentials it needs, tools, `$command`, panel). Nothing runs until you accept — and freshly installed plugins start **disabled** until you turn them on.\n\n**Using a plugin with `$command`:**\n\n- Type its command at the **start** of a message — e.g. `$cindy-art a poster for…`. Only enabled plugins that declare that command match.\n- You don't have to use `$command`: the agents can also pick an enabled plugin's tools on their own when a task calls for it.\n\n**The permission / sandbox model (why plugins are safe):**\n\n- A plugin's sandbox has **no filesystem, no network, and no direct system access** by default. It can only ask the host to do things it declared up front in its manifest, and the host enforces each one.\n- **Network** access is limited to a domain allowlist the plugin declares; the host proxies every request. Credentials never enter the sandbox — the host injects them only into the declared requests.\n- Because of this, a plugin **can't see your files** on its own. A file reaches a plugin only when you or the agent explicitly hand one over (an image attachment, or a directory the host passes through after an ownership check).\n\n**Configuring a plugin's account / API key:**\n\n- Do it on the plugin's **detail page**. Plugins that use a key (web-search, GitHub/GitLab tokens) have a field there; the value goes straight into the host's encrypted store and can't be read back.\n- Plugins that use OAuth (for example Filo Google) show a **Connect account** button instead.\n\n**Notes:**\n\n- Enabling or disabling a plugin takes effect on the **next** agent query, not retroactively in the current turn.\n- Skills (`/` slash commands) and Plugins (`$` commands) are two tabs of the same management surface — see the Skills topic for skills.",
  },
  {
    id: "providers",
    title: "Model providers and Cindy AI",
    summary: "Cindy AI is auto-provisioned after sign-in for pay-per-token model access; manage it (and custom providers) in Settings > Model Providers.",
    tab: "providers",
    content: "Claude Code, Codex, and lightweight utility models can use **Cindy AI** for pay-per-token model access. Voice input is included for signed-in Cindy users and uses the separate voice service; it does not require or receive a Cindy AI key.\n\n**Cindy AI:**\n\n- Open **Settings > Model Providers** (the tab is labeled \"Providers\"). Cindy AI is pinned at the top.\n- You don't paste a key — it's **provisioned automatically after you sign in**. The page fetches its credentials for you (\"Fetching credentials…\" while it does).\n- Controls: **Refresh credentials**, **Rotate key**, and **Disconnect** (which clears the Cindy AI key saved locally). Because it's tied to your account, it's available on each device you sign into.\n\n**Status badges:**\n\n- **Connected** — this device has usable Cindy AI credentials.\n- If it's still fetching or disconnected, use Refresh credentials / sign in.\n\n**Custom providers:**\n\n- You can also add your own provider (a base URL + key) — that's the flow with a paste-a-key dialog. Cindy AI itself never uses that dialog.\n\n**Notes:**\n\n- Direct service keys such as Brave Search and Tavily are **not** managed here — they live on the matching plugin detail pages under Plugins (Cindy Web Search).\n- Rotating or replacing credentials takes effect for new turns; in-flight requests finish on the previous key.",
  },
  {
    id: "remote",
    title: "Remote workspaces and device control",
    summary: "Run agents on a remote machine over SSH, and let another signed-in device (e.g. your phone) control this desktop — both in Settings > Remote & device control.",
    tab: "remote-control",
    content: "Settings > **Remote & device control** has two parts: **SSH remote workspaces** (run an agent on another machine) and **My Devices** (let other devices you're signed into control this one).\n\n**SSH remote workspaces:**\n\n- **Add a host** with the **+** button: give it an alias, hostname (IP or domain), user, port (default 22), and an auth method (SSH agent or a key file). Hosts from your `~/.ssh/config` also show up automatically; you can re-read that file at any time.\n- Once a host connects (status dot turns green), expand it to:\n  - **Install the agents** (Claude Code / Codex) on the remote machine, and run a quick test prompt against them.\n  - **Start a remote session** — enter a working directory on the remote (defaults to `~`); the app creates the directory if needed and opens a normal session whose agent runs **on the remote machine** with that remote working directory. It appears in your sidebar like any other session.\n- The agent-facing SSH tools (via the `cindy-ssh` plugin) reuse these **same** configured hosts — if a tool reports the host isn't found, add it here first.\n\n**My Devices (controlling this desktop from another device):**\n\n- **No manual pairing** — any device signed into the **same Cindy account** is discovered automatically and appears in the list (this is how the phone app connects to your desktop).\n- The **top card is this machine**: rename it, see its relay-connection status, and toggle the master switch **\"allow other devices to control this machine\"**.\n- Each **other device** has two independent switches: **you control it** (outbound) and **allow it to control this machine** (inbound). You can rename devices, and delete ones that are offline.\n\n**What a controlling device can and can't do:**\n\n- A controlling device (like the phone app, which is control-only — it has no local agent or database of its own) can drive sessions: create / close / fork them, send and steer input, approve interaction prompts, switch model / effort / permission mode, rewind, manage collaboration workers and scheduled tasks, and read session / message lists.\n- It can **not** touch auth or API keys, change global settings, write the database directly, trigger updates, or run local shell / file dialogs on the controlled machine. A new remote session's working directory must be currently accessible on the controlled desktop; disconnected network drives are rejected.\n\n**Notes:**\n\n- Remote project sessions **can** be the lead in a collaboration workflow — workers then run on the same remote host (see the Collaboration topic).\n- Device control works only between devices on the **same account**; the relay just routes between them — your session data isn't stored on it.",
  },
  {
    id: "reviewing-changes",
    title: "Reviewing the agent's file changes",
    summary: "Open the Review tab in the right sidebar to see what changed as git-style diffs, and stage / revert hunks.",
    content: "When you want to see what the agent changed, use the **Review** tab in the right sidebar — it shows the working directory's changes as git diffs.\n\n**Opening it:**\n\n- In the right sidebar, open the **+** menu and pick **Review** to add the Review tab.\n- The diffs come from a real **git diff** of your working directory against a base ref — not from replaying the agent's individual Edit / Write calls. You can change the base branch it compares against.\n\n**Inside the tab:**\n\n- Changed files are shown as diffs, **expanded by default**; collapse the ones you don't care about.\n- Toggle between **unified** and **split** views. There's also inline word-diff, word-wrap, hide-whitespace, a file tree, and a rich Markdown preview for `.md` files.\n- Colors follow git convention: **red removed / green added**.\n\n**Acting on changes:**\n\n- Each hunk has action pills to **stage / unstage / revert** it — so you can review the agent's edits and selectively keep or roll back individual hunks right from the tab.\n\n**Notes:**\n\n- Because the tab reflects real git state, it shows everything in your working tree — including edits you made yourself — not only what the agent touched this turn.\n- This is separate from **Plan mode** (approving a plan before work starts) and from the **collaboration** worker panes.",
  },
  {
    id: "scheduler",
    title: "Scheduling automated tasks",
    summary: "Run a prompt automatically on a schedule (recurring or one-off) against a chosen working directory.",
    content: "The Scheduler runs prompts for you on a schedule — recurring (cron-style) or one-off (a specific time).\n\n**Creating a task:**\n\n- Open the Scheduler.\n- Give the task a **prompt** (what the agent should do), a **working directory** (where it runs), and a **schedule** — recurring (interval / cron) or one-time (specific datetime).\n- Pick which **agent** runs it (Claude Code or Codex) and the model / mode you want.\n- Save. The task fires according to its schedule.\n\n**Managing existing tasks:**\n\n- Edit (prompt, schedule, working directory).\n- **Pause / resume** without losing the task definition.\n- Delete.\n\n**Run history:**\n\n- Each task tracks its past runs — when it fired, whether it succeeded, the resulting session (you can open it to see what the agent did).\n\n**Notes:**\n\n- The task only fires while the desktop app is running on your machine — it's a local scheduler, not a cloud cron. Closing the app means tasks won't fire until you reopen it.\n- Tasks run as normal sessions under the hood; you can find their resulting sessions in the sidebar (often grouped by the scheduler).\n- If you have two instances open on the same account (e.g. dev + release), they coordinate so a due task fires only once — you won't get duplicate runs.\n- A failing task shows the failure in run history but doesn't auto-pause itself — you may want to disable a chronically failing task manually.",
  },
  {
    id: "sessions-and-chat",
    title: "Sessions and the chat thread",
    summary: "Create sessions, send / stop / queue messages, clear context, and switch between sessions in the sidebar.",
    content: "Each **session** is a persistent chat thread driven by **one agent** (Claude Code or Codex). All your chats live as sessions in the left sidebar.\n\n**Starting a session:**\n\n- Click **+ New** at the top of the sidebar.\n- Pick the agent (Claude Code or Codex) and — for code work — a working directory.\n- Pick a model in the composer toolbar if you want something other than the default.\n- Type your first message and send.\n\n**During a turn:**\n\n- While the agent is replying, **Send** becomes **Stop** — click it to interrupt the current turn cleanly.\n- You can keep typing while the agent is busy — follow-ups are **queued** in order and sent one after another when the previous turn finishes.\n\n**Conversation controls:**\n\n- **`/clear`** — clears the current session's conversation context in place (resets messages and state without creating or switching to a new session). The session itself stays in the sidebar; only its content is wiped.\n- **`/compact`** — ask the agent to summarize and compress earlier turns into a shorter context (Claude Code).\n- **Edit** — you can edit your **last** user message. Doing so **rewinds** the conversation: it drops that message and everything after it, then resends your edited message. In sessions with file-rewind support (local git working directories with savepoints), file changes are also rolled back. In sessions without savepoints (remote sessions, non-git directories, or Codex sessions that haven't created savepoints), only the conversation is rewound — filesystem changes remain.\n\n**Switching between sessions:**\n\n- Click any session in the sidebar to switch to it. State is persisted — each session remembers its agent, model, working directory, attachments, etc.\n- Keep **unrelated tasks in separate sessions** — sharing one session for everything dilutes the agent's context and confuses it.\n\n**Notes:**\n\n- Sessions are **per local user** — they live in the local SQLite DB at `<userData>/cindy-<userId>.db` (installs migrated from the legacy app get their sessions copied from the old `xdt-maker-<userId>.db` on first login). They don't sync to other machines automatically.\n- If you have two desktop instances open (e.g. dev + release) on the same user account, both read/write the same DB and will see each other's sessions.",
  },
  {
    id: "sidebar",
    title: "Finding and organizing sessions",
    summary: "Browse, group, filter, search, and pin sessions or projects in the left sidebar.",
    content: "The left sidebar lists your sessions. It's the main way to navigate and organize.\n\n**Grouping:**\n\n- Switch grouping between **by project** (working directory) and **by date** — those are the two options.\n\n**Filtering:**\n\n- **Status** — active / archived / all.\n- **Project** — multi-select working directories, or \"all\".\n- **Agent** — Claude Code / Codex / all.\n- **Recent activity** — last 1 day / 3 days / 7 days / 30 days / all.\n\n**Pinning:**\n\n- Right-click a session and pick **Pin** to keep it at the top. There's no cap on the number of pinned sessions; a newly pinned session goes to the **front** of the pin order (most recent pin first).\n- Right-click a pinned session and pick **Unpin** to remove the pin.\n- Open a project's overflow menu and pick **Pin project** to move the whole project into **Pinned**. Project pins are independent from session pins, remain available after a restart or project rename, and can be expanded or collapsed inside **Pinned**.\n- Pinned projects and sessions share one draggable order. Newly pinned items move to the front, while the other pinned items keep their relative order. Use **Unpin project** to return a project to its position under the current project sorting mode.\n- The **Pinned** section supports the same **Text**, **List**, and **Card** display modes for both sessions and projects.\n\n**Searching:**\n\n- The search box matches against the **session title only** — not message content and not the working directory path. If you can't remember a session's title, browse via project / date / agent filters instead.\n\n**Right-click actions:**\n\n- On a regular session: **Pin / Unpin**, **Rename**, **Move to project** (submenu), **Copy conversation link** (a `cindy://session/<id>` deep link), **Open in new window**, **Archive**, **Delete**.\n- On an archived session: **Rename**, **Unarchive**, **Copy conversation link**, **Delete**.\n\n**Session statuses:**\n\n- Sessions are **active**, **archived**, or **deleted** — there are no other states. Archiving hides a session from the default view without removing it; delete is unrecoverable from the UI.\n\n**Notes:**\n\n- Click the sidebar collapse arrow to shrink it to an icon-only strip — that's purely visual, you don't lose any features.\n- **Pin state and order** are persisted in `<userData>/sidebar-settings.json` and are shared across windows. **Filter settings** are stored per-window (in that window's local storage), so they don't sync between, say, a dev window and the installed app.",
  },
  {
    id: "skills",
    title: "Skills (reusable agent capabilities)",
    summary: "Browse, install, publish, and update agent skills; installed skills appear as \"/\" slash commands and are shared across Claude Code and Codex.",
    content: "Skills are reusable agent capabilities you package as a folder and load into your sessions. They're managed on the same page as Plugins — **Skills** and **Plugins** are two tabs of one management surface (the Skills tab is the in-app browser for finding, installing, publishing, and updating skills).\n\n**Skills work with both agents:**\n\n- Skills are **engine-shared** — the same installed skill is available to both Claude Code and Codex, so you install it once.\n\n**Where skills live on disk:**\n\n- Global (available to every session): the shared root `~/.agents/skills/<name>/`, which is cross-linked to `~/.claude/skills/` and `~/.codex/skills/` so every engine sees the same skills. On Windows that's under `C:\\Users\\<you>\\`.\n- Project-scoped (only inside one working directory): `<working-dir>/.agents/skills/<name>/` or `<working-dir>/.claude/skills/<name>/`.\n- Each skill is its own folder with a required `SKILL.md` at the root (the prompt / spec the agent reads). Sibling files and subfolders in that folder are also visible to the agent.\n\n**Using an installed skill:**\n\n- Type `/` in the composer to open the slash-command palette; your installed skills show up there alongside built-in and agent commands. Pick one to run it.\n\n**Publishing your own skill:**\n\n- Find the publish action on the Skills page and point it at the skill's local folder. It zips the folder and uploads it — reading your directory in place, without copying or moving anything.\n- On first publish, you set the skill's **visibility**: PUBLIC (anyone in the org) or DEPARTMENT_SCOPED (only the departments you choose).\n- The local registry records what you published, so the app knows it's \"yours\" for future updates.\n\n**Updating an already-published skill:**\n\n- On your own skill's detail page you'll see a **发布新版本** (Publish New Version) button.\n- Edit the local folder however you want and publish — the version is **auto-incremented server-side**, you don't pick a number.\n- **Old versions stay live alongside the new one.** Users who already installed an older version keep it until they choose to update.\n- The display name and description are sent on every republish, so editing those metadata fields just means doing a republish.\n\n**Managing an already-published skill:**\n\n- Change who can see or use your skill from the management menu — public, shared with selected teams / departments, or private to you.\n- **Unpublish** makes it private again and returns ownership to your personal scope; it does not remove copies other users already installed.\n- **Authorship is fixed.** Only the original author's account can publish new versions of a given skill (enforced server-side as `NOT_AUTHOR`).\n\n**Browsing and installing others' skills:**\n\n- The marketplace lets you install skills into your global skills directory. You can request a specific version on install; the local registry tracks which version you have.\n\n**Notes:**\n\n- A skill folder without a `SKILL.md` at its root won't be picked up (lowercase `skill.md` is also accepted).\n- Project-scoped skills only show up in sessions whose working directory matches — useful for skills tied to a particular repo's conventions.\n- Editing files inside an installed skill folder takes effect on the next session start; you don't need to reinstall.\n- Uninstalling only removes your local copy. For your own published skills, use unpublish or manage visibility to change market availability.",
  },
  {
    id: "updates-and-about",
    title: "App updates, version and logs",
    summary: "How auto-updates apply; where to find the app and agent versions, the debug-log toggle, and the logs folder.",
    tab: "about",
    content: "Cindy checks for updates automatically and downloads them in the background. When an update is ready, you're prompted to **relaunch** the app to apply it.\n\n**Settings > About:**\n\n- **App version** — the desktop app's release.\n- **Claude Code version** / **Codex version** — the bundled agent CLIs the app ships with (these update with the app, not separately).\n- **Auto-relaunch when idle** — an optional toggle that lets a downloaded update apply itself during idle time instead of waiting for you to click relaunch.\n- **Debug log toggle** — turn on more verbose logging when reproducing a problem. Leave it off for normal use.\n- **Open logs folder** — opens the logs directory in your OS file browser, useful when you need to attach logs to a bug report.\n- **Storage** — a storage-management card for reviewing / clearing local app data.\n\n**Notes:**\n\n- By default, a downloaded update only triggers a relaunch prompt — it won't restart you unexpectedly. If you enable **Auto-relaunch when idle**, Cindy will restart itself automatically once the idle and busy gates pass, without an additional prompt.\n- If you want to skip auto-update temporarily, you can dismiss the relaunch prompt; the update applies next time you start the app.\n- The Claude Code / Codex versions you see here are what the **app's bundled agents** run as — they're independent from any Claude / Codex CLI you have installed globally on your shell.",
  },
  {
    id: "voice-input",
    title: "Using voice input (dictation)",
    summary: "Push-to-talk dictation that inserts ASR text into the focused composer; configure shortcut, mic, language, refinement, and dictionary in Settings > Voice Input.",
    tab: "voice-input",
    content: "Voice input lets you dictate instead of typing. Hold a shortcut, talk, release — the recognized text is inserted into whichever composer has focus.\n\n**Activation:**\n\n- Default shortcut: **Alt+Space** on macOS, **Ctrl+Shift+Space** on Windows. The shortcut is customizable on macOS and Windows. On Linux, global voice shortcuts are not supported yet (the setting is hidden).\n- The shortcut is **global** — it works even when Cindy isn't the focused app, so you can dictate into the composer from anywhere.\n- It's **push-and-hold**: press to start listening, release to stop and submit.\n\n**Settings:**\n\n- **Microphone** — pick a specific device or leave on \"auto\" (system default).\n- **Language** — auto / 中文 (zh-CN) / English / 日本語 / 한국어. Those are the supported recognition locales — there's no free-form locale field.\n- **Refinement** — optional LLM post-process that cleans up the raw ASR text (fixes punctuation, removes ums, joins fragments). You can write a custom refinement prompt to bias it toward your preferences.\n- **Mute system audio while recording** — toggle; reduces feedback / echo from your speakers picking up onto the mic. On by default.\n- **Fast activation** — toggle; keeps the mic warm so press-to-talk has lower latency at the cost of slightly more background mic usage. Off by default.\n- **Interaction sound** — a short sound cue when dictation starts / stops. On by default.\n\n**Dictionary (improves recognition of your terms):**\n\n- **Manual entries** — type terms you want recognized (people's names, product names, jargon).\n- **Learned entries** — when you edit the text shortly after dictation lands in the composer, the external-edit inspector picks up your corrections and feeds them back as automatic dictionary entries. Both manual and learned entries feed the refinement pass.\n\n**Permissions (macOS):**\n\n- **Microphone** — required; the page prompts the OS dialog if you haven't granted it.\n- **Input Monitoring** and **Accessibility** — may be required depending on how the global shortcut is captured. The page shows each permission's status (granted / denied / unknown) so you know what to grant in System Settings.\n\n**Notes:**\n\n- Push-and-hold is the primary activation mode; **Fast activation** changes the warm-up behavior, not the trigger itself.\n- If the global shortcut fails to register (e.g. it's already taken by another app like an IME), you'll see a warning in the page; pick a different shortcut.\n- If the connection drops mid-dictation, the text recognized up to that point is kept — it lands in the composer (or stays in the overlay with a copy button) alongside the error, instead of being discarded. That salvaged text is raw ASR output: refinement needs the connection that just failed, so it is skipped.",
  },
  {
    id: "worktrees",
    title: "Working in an isolated git worktree",
    summary: "Opt a session into an isolated git worktree so its changes stay on their own branch and folder.",
    content: "A session can run inside its own **git worktree** — a separate working copy of the same repository on its own branch — so the agent's edits don't mix with your main checkout.\n\n**Opting in:**\n\n- In the new-chat view, click the working-directory chip → the **gear** icon to open the Advanced popover → tick the **worktree** checkbox.\n- The worktree is created **on first message send**, not on session create. An overlay shows the status (creating / ready / failed).\n\n**Where it goes:**\n\n- Folder: `<repo>/.cindy-worktrees/<auto-generated-name>/` — kept inside the same repo so git tooling can find it. Worktrees created by older versions under `.xdt-worktrees/` remain supported.\n- Branch: `xdt/<auto-generated-name>` — auto-created off the source branch you pick, which defaults to the repo's current branch (HEAD).\n\n**Lifecycle:**\n\n- Created on first agent turn that needs files.\n- Survives across app restarts as long as the session is around. Transient closes (`/clear`, reconnecting, quitting the app) do **not** remove it.\n- The folder is removed only when you explicitly **delete or archive** the session; the **branch is preserved** (no force-delete), so you can keep the work or merge / cherry-pick from it later.\n- Uncommitted changes inside the worktree are **auto-stashed** before the folder is removed, so nothing is silently lost. Removal is also skipped if you leave a `.worktree-keep` file in it or another live session is still using it.\n\n**When the option is disabled:**\n\n- Git is not installed on your machine.\n- The chosen working directory isn't a git repository.\n- The chosen directory is already inside an existing worktree (no nesting).\n\n**Error cases (shown on the overlay):**\n\n- `permission-denied` (can't write into the repo dir), `git-crypt-locked` (repo uses git-crypt and isn't unlocked), `dubious-ownership` (git's safe.directory check tripped), `lfs-error`, `not-a-git-repo`, `git-not-installed`.\n\n**Notes:**\n\n- The agent's **Read / Write / Bash** tools automatically operate inside the worktree path — you don't need to switch directories manually.\n- Because the branch survives the worktree, \"did the agent's changes go anywhere?\" is always answerable with `git branch --list 'xdt/*'`.\n- Worktrees are a per-session opt-in — they're not on by default and they don't change non-worktree sessions in any way.",
  },
];
